====
Primitives
====

√+ ? .+ ? 1 ×3 4

÷2 8 # Line
@x # these
1 # up

---

(source_file
  (primitive (monadic_glyph))  
  (primitive (dyadic_glyph))  
  (primitive (stack_glyph))  
  (primitive (stack_glyph))  
  (primitive (dyadic_glyph))  
  (primitive (stack_glyph))  
  (literal (number (integer)))
  (primitive (dyadic_glyph))  
  (literal (number (integer)))
  (literal (number (integer)))

  (primitive (dyadic_glyph))
  (literal (number (integer)))
  (literal (number (integer)))
  (literal (char))
  (literal (number (integer)))
)
